# Agent reports archive

Verbatim or condensed reports from sub-agent task invocations, ordered
chronologically. Primary source material for the "AI-assisted development"
chapter of the thesis.

## Format

Each file is one agent task. Filename pattern:
`<milestone>_<short-description>.md`. Each report includes:

- Task brief summary (what was asked)
- Agent's report (verbatim where preserved; condensed otherwise)
- Wall-clock duration and token usage if known
- Files touched
- Notable findings

## Coverage

Earlier milestones (M0–M9) have reports preserved in conversation
transcript but only partially reconstructed here. The discipline starting
at M10 is: every agent's report gets a verbatim copy in this directory at
the time it lands.

## Files

| File | Milestone | Description |
|---|---|---|
| `m8_cranelift_jit.md` | M8 | Cranelift AOT integration. fib(30): 931ms → 14.6ms. |
| `m9_full_jit_coverage.md` | M9 | Heap mutation + fields + virtual calls. All 16/16 cells win. |
| `m10_csv_aggregator_stress.md` | M10 prep | First real-world program. Found BUG-001 (nullable f64). |
| `m10_ab_nullable_audit.md` | M10 | Audit found 4 more nullable miscompiles in codegen.rs. |
| `m10_c1_game_of_life_sudoku.md` | M10 | Game of Life + Sudoku. Zero new bugs found. |
| `m10_c2_json_parser_markov.md` | M10 | JSON + Markov. **Found 8 bugs incl. is-not-none INVERTED.** |
| `m10_c3_kvstore_brainfuck.md` | M10 | KV store + Brainfuck. Found 3 typecheck/native bugs. |
| `m10_fix_pass.md` | M10 | Fixed critical bugs from C2/C3 reports. |
| `m11_c4_lambda_calc_calculator.md` | M11 | λ-calc + calculator. Calculator hits BUG-026 hard. |
| `m11_c5_tictactoe_levenshtein.md` | M11 | Tic-tac-toe + Levenshtein. Found `i32(i64)` silent truncation. |
| `m11_c6_lisp_interpreter.md` | M11 | Toy Lisp. **Found N1 (vtable >4 slots) + N2 (deterministic heap corruption).** |
| `m11_class_system_fix.md` | M11 | Class-system overhaul. **3 converging root causes for vtable-mod-4; class_id ↔ type_id collision in op_new.** Provisionally closes BUG-026/027. |
| `m12_regex.md` | M12 | Thompson-NFA regex engine. **Zero new bugs.** Confirmation that M11 holds for sealed/8-subclass/6-vmethod hierarchies with class-ref fields. |
| `m12_dijkstra.md` | M12 | Dijkstra + min-heap PQ. **Zero new bugs.** Confirmation for `final class` with parallel nested-list fields + recursive method calls. |
| `m12_btree.md` | M12 | In-memory B-tree (order 4). Found **BUG-034** (str != str silent miscompile — same shape as BUG-008) and **BUG-035** (and/or no short-circuit). BUG-034 fixed inline. |
| `m12_torture.md` | M12 | Torture test (250 sequential runs across calculator/json_parse/lisp). **BUG-026 + BUG-027 CONFIRMED FIXED.** |
| `m13_short_circuit.md` | M13 | Short-circuit `and`/`or` (BUG-035 fixed). First mid-expression CFG manipulation; documents the slot-phi pattern for M15 try/except. |
| `m14_tuples.md` | M14 | Tuples + destructuring. Heap-allocated synthetic class layouts; zero new VM opcodes. Eliminates highest-frequency workaround. Incidentally fixed an assert(cond, msg) IR-tuple-allocation crash. |
| `m15_try_except.md` | M15 | try/except/finally + raise. **BUG-025 closed.** Lazy materialisation of exception objects; per-function JIT carve-out. |
| `m16_match_isinstance.md` | M16 | `isinstance(x, T)` + `match case Constructor()`. Eliminates the `kind: i32` discriminator workaround. Flow-narrowing for isinstance mirrors is-not-none. |
| `m17_generics.md` | M17 | Generic free functions with call-site monomorphisation. Lazy worklist (Pass 2.6 seeds; Pass 3.5 drains to fixpoint). Generic classes deferred to v0.2. |
| `m18_algorithms_lib.md` | M18 | R1 — Generic algorithms library. 8 generic fns × 5 primitive types + Tuple + class. **Zero new bugs.** |
| `m18_json_parse_v2.md` | M18 | R2 — JSON parser rewritten with M13-M17 surface. All 8 M10 workarounds eliminated. **Zero new bugs.** |
| `m18_expr_interp.md` | M18 | R3 — Expression interpreter. Found **BUG-036** (divzero name mismatch). Plus 7 archived edge-case probes. |
| `m18_graph_lib.md` | M18 | R4 — Generic graph algorithms. M17 worklist verified: 8 monomorphic instantiations drained to fixpoint, 2 discovered transitively. **Zero new bugs.** |
| `m19_import_sys.md` | M19 | Import machinery + sys module. First real module system. Non-catchable VmError::Exit. argv plumbed end-to-end. |
| `m20a_os_path_io.md` | M20a | os + path + io stdlib (23 NativeFns). Found **BUG-037** incidentally (?? null-coalesce always-fallback). |
| `m20b_time_random_math.md` | M20b | time + random + math stdlib (31 NativeFns). Numerical Recipes LCG; hand-rolled civil_from_days for ISO formatting. |
| `m20c_json_re.md` | M20c | json + re stdlib (12 NativeFns). serde_json + regex crates added only to vm/Cargo.toml. **Phase 1 stdlib complete.** |
| `m22_p2a.md` | M22 P2A | argparse + collections + csv (26 NativeFns, 250-280). First parallel worktree-isolated stdlib agent. ArgParser as Dict[str, str] shim pending v0.3 stdlib classes. |
| `m22_p2b.md` | M22 P2B | base64 + hashlib (9 NativeFns, 290-304). 5 new crate deps in vm/Cargo.toml: base64, sha1, sha2, md-5, hmac. Zero incidental bugs. |
| `m22_p2c.md` | M22 P2C | itertools + statistics (20 NativeFns, 310-329). Monomorphic per-type variants matching M20b random.*. Zero incidental bugs. |
| `m22_p2d.md` | M22 P2D | struct + urllib_parse (18 NativeFns, 330-347). `str`-as-byte-buffer encoding for binary IO. Self-fixed an `OBJECT_HEADER_SIZE` mismatch. |
| `m23_p3a_a.md` | M23 P3a-A | subprocess + pathlib (20 NativeFns, 350-389). i64-handle process registry; flat-fn pathlib pending v0.3 stdlib classes. |
| `m23_p3a_b.md` | M23 P3a-B | datetime (22 NativeFns, 390-411). Hand-rolled `civil_from_days` + platform-specific local_offset via FFI; no chrono dep. |
| `m23_p3a_c.md` | M23 P3a-C | threading.Lock + Semaphore + queue.PriorityQueue (18 NativeFns, 420-437). Three new SharedVm slot tables. Incidental resolver fix for stdlib-module shadowing of legacy `from X import Y` prelude bindings. |
| `m23_p3a_d.md` | M23 P3a-D | sqlite3 (9 NativeFns, 440-448) via rusqlite-bundled. Stringified result cells; v0.3 typed rows. |
| `m24_a.md` | M24-A | Stress: background job_scheduler (subprocess + threading.Lock + queue.PriorityQueue + datetime). 9/9 probes PASS, 0 bugs. |
| `m24_b.md` | M24-B | Stress: event_log CLI (sqlite3 + datetime + argparse + io + pathlib + re). 14/14 probes PASS. **Found BUG-039** (`k in Dict[str,*]` always false) + segfault sibling on Dict[i64,_]. |
| `m24_c.md` | M24-C | Stress: parallel test_runner (subprocess + threading + queue + sqlite3 + time). 10/10 PASS. Real parallelism verified: 3.62×-5.75× speedup at N=4. |
| `m24_d.md` | M24-D | Stress: fs_migrator (pathlib + os + datetime + subprocess + io). 10/10 PASS. Documented missing stdlib primitives (os.mtime, os.size, re capture groups). |

(M25 was a single-conversation orchestrator refactor — no sub-agents,
no agent report. See `docs/thesis/milestones/m25_unified_cli.md`.)

(M26 was a single-session extended-benchmark addition — no sub-agents.
See `bench/EXTENDED_REPORT.md`.)

| `m27_p3c_a.md` | M27 P3c-A | shutil + tempfile (NativeFns 450-472, §9.30/9.31). Closes the v0.2 gap M24-D documented (no recursive rmdir). `shutil.which` does Windows .exe lookup. |
| `m27_p3c_b.md` | M27 P3c-B | glob + fnmatch (NativeFns 480-486, §9.32/9.33). Uses Rust `glob` crate; `fnmatch.translate` hand-rolled (~50 LOC). |
| `m27_p3c_c.md` | M27 P3c-C | gzip + zlib + bz2 (NativeFns 500-510, §9.34/9.35/9.36). Round-trip + level + checksum (crc32/adler32). **Found and worked around a bzip2 write-side decoder hang** on malformed input; switched to read-side. |
| `m27_p3c_d.md` | M27 P3c-D | zipfile + tarfile (NativeFns 520-535, §9.37/9.38). Uses `zip` + `tar` Rust crates. Two slot tables per format (read/write). Tar writer is enum-typed for plain/gz/bz2 modes. |
| `m27_p3c_e.md` | M27 P3c-E | logging (NativeFns 550-560, §9.39). Flat global-logger surface (v0.3 will add class-shaped Logger/Handler/Formatter on top of stdlib classes). Hand-rolled timestamp formatting via M20b's civil_from_days. |
| `m28_p3b_a.md` | M28 P3b-A | socket (NativeFns 570-588, §9.40). TCP+UDP raw + listen/accept + DNS. No new crate deps. **Self-caught a deadlock** in initial impl (Arc<T> needed instead of holding outer Mutex across blocking I/O); shipped a focused fix-up commit. |
| `m28_p3b_b.md` | M28 P3b-B | ssl (NativeFns 600-609, §9.41). TLS-over-TCP via rustls + ring + webpki-roots. Hand-rolled ~30-LOC CN extractor vs pulling in x509-parser. **Best Lesson 1 discipline of any M28 agent**: green-build checkpoint at ~30% of budget. |
| `m28_p3b_c.md` | M28 P3b-C | http_client (NativeFns 620-649, §9.42). HTTP/1.1 via ureq (bundles rustls + webpki-roots). Stateless handlers, fresh socket per call. Loopback test uses hand-rolled TcpListener-based mock HTTP server. |
| `m28_5_p3b_d.md` | M28.5 P3b-D | Server-side TLS extension to ssl (NativeFns 610-612, §9.41 amended in place). Option A — parallel tls_server_streams table; server handles ≥ 1M, client < 1M. Single agent, clean Lesson 1 + 2 discipline, zero conflicts at integration. |
| `m29_webserver.md` | M29 | **Largest single-program stress test of the project.** 1,446 LOC Sinatra-shaped HTTP/1.1 + HTTPS framework + TODO demo. **Zero new bugs in M28/M28.5 — first stress round with zero finds.** 4 v0.2 ergonomics gaps documented (no JsonValue tree, `from` keyword, `T?` unwrap, BUG-039 for non-str Dict). ~2200 req/s HTTP performance, within 2× of Flask+gunicorn. 4 commits, all before 80% budget. |
| `m29_5_webserver_roundout.md` | M29.5 | Tier 1 round-out of the M29 framework: HTTP keep-alive + chunked TE + multipart + graceful shutdown + HTML error pages. ~1,000 LOC added. **Found BUG-040** (`socket.close_listener` doesn't unblock blocked `accept` — M28 P3b-A handler Arc-clones the listener and drops the mutex before the blocking syscall). Workaround: self-connect from shutdown timer. |
| `m30_bug028.md` | M30 BUG-028 | Lexer line continuation across infix operators. 95-LOC additive fix in `compiler/src/lexer.rs` tracking the last significant token; suppress NEWLINE if it's a binary operator needing a right operand. 11 new regression tests. Last open frontend bug. |
| `m30_bug040.md` | M30 BUG-040 | `socket.close_listener` now wakes blocked `accept()` via belt-and-braces `shutdown(fd, SHUT_RDWR)` + self-connect (with wildcard-bind rewrite to loopback). **Cross-platform finding**: Windows winsock does NOT wake `accept` from `shutdown` alone (Microsoft KB-179942) — self-connect is essential on Windows. |
| `m31_generic_classes.md` | M31 | **First v0.3 feature**: generic classes (`class Box[T]:`, `Pair[K,V]:`, `Stack[T]:`). Extends M17 worklist to classes. Per-instantiation type_id + method bodies. New IR Pass 2.7 + 3.6 running to joint fixpoint with M17's Pass 2.6 + 3.5. Constructor-site inference (no explicit `Box[i64]()` — v0.4). +8 tests. Lesson 1 streak: 11 consecutive clean agents. Unblocks v0.3 stdlib classes (typed JsonValue, Request/Response, re.Pattern). |
| `m32_async.md` | M32 | **Second v0.3 feature**: async I/O — Shape A thread-backed Future façade. New `asyncio` module (IDs 700-714) + async socket variants (720-722). Future[T] as TypeCtor (joining Channel/Atomic/Dict/List), special-cased .await() dispatch — agent's call to NOT use M31's machinery, ~25 LOC vs full worklist. Demo: async_echo_server.spy with 3 concurrent clients. +9 tests. v0.4 swaps internals for mio event loop. |
| `m33_precise_gc.md` | M33 | **Third v0.3 feature**: precise GC stack maps — shadow-stack fallback. JIT spills register variables into per-function stack slot + pushes (buf, len) window before each heap-allocating helper; GC reads windows during collection. Removes M9 in_jit pause. **Zero cherry-pick conflicts with M32** despite both touching interp.rs + lib.rs — cleanest parallel integration in project history. +4 tests. v0.4 swaps to real Cranelift safepoints. |
| `m34_json_value.md` | M34 | **First stdlib classes in v0.3**: sealed `JsonValue` + 6 subclasses (JNull/JBool/JInt/JFloat/JString/JList/JObject), `json.parse`/`stringify`, constructor helpers. Registered in prelude (scope-down) rather than building module-level class infra (v0.4). JList[JsonValue] worked first-try under M11+M31; GC root scanning free via existing GcKind. Closes the #1 M29 ergonomics gap (~50→~10 LOC). +13 tests. Lesson 1 streak: 14. |
| `m35_p4a_re_pattern.md` | M35 P4-A | Compiled `re.Pattern` class (NativeFn IDs 790-799). `re.compile(s) -> Pattern` is the only entry point; methods `matches / find / find_all / replace / replace_all / split / source`. Single i64 handle field indexing into `SharedVm.p4a_compiled_regexes`. Compile-once-reuse-cheaply for hot loops. +12 tests (10 vm + 2 compiler). |
| `m35_p4b_sqlite_class.md` | M35 P4-B | Typed `sqlite3.Connection` + `Cursor` classes (NativeFn IDs 800-819). `sqlite3.open(path) -> Connection` / `conn.query(sql) -> Cursor`. Cursor methods `fetchone / fetchall / column_names / row_count`. Flat M23 P3a-D surface preserved; new class shape wraps the same slot table. +13 tests (11 vm + 2 compiler). |
| `m35_p4c_hashlib_streaming.md` | M35 P4-C | Streaming `hashlib.Hasher` class (NativeFn IDs 820-829). `hashlib.new("sha256") -> Hasher`; methods `update / hexdigest / digest / copy / reset / name`. Wraps the M22 P2B one-shot surface (`hashlib.sha256(data)` etc.) without replacing it. +8 tests (6 vm + 2 compiler). |
| `m36_stdlib_class_refactor.md` | M36 | **`StdlibItemKind::Class` infrastructure refactor.** Single agent, 2 commits. New `Class { class_id: ClassId }` enum-with-payload variant (chosen over `Option<ClassId>` field to avoid touching 345 existing construction sites). All 11 M34/M35 stdlib classes now published through their home modules as Class items; `from MOD import X as Y` resolver branch extended to bind aliased imports. **Honest scope-down**: prelude bindings retained — M34/M35 tests reach names by bare lookup, hard removal would regress 39 tests. Phase D annotated the legacy "prelude wins" branch for future deletion. Tests unchanged at 723 (pure refactor). Lesson 1 streak: 18. |
| `m37_tabular.md` | M37 | **First Pandas-shaped data package for v0.3** (`tabular` stdlib module). Single big agent, 5 phase commits (A-E), ~2800 LOC. 6 new classes registered MODULE-SCOPED via M36 path (first stdlib package on the post-M36 canonical path): sealed `Column` + 5 subclasses (i64/f64/str/bool/datetime) + `DataFrame`. Per-column null mask NA semantics. Surface: `tabular.col_*` factories, `from_columns / from_rows`, `read_csv / write_csv / from_sql` (reuses M35 Cursor), `df.show(n)` ASCII table, per-column comparisons → ColumnBool masks, mask combinators, `df.filter / select / drop / head / tail`, stable `df.sort_by`. STOP CRITERIA cut Phase C `between / ne / ge / le / starts_with`; M38 follow-up. NativeFn IDs 830-877. +21 tests; +1 demo (`examples/tabular_demo.spy`). Lesson 1 streak: 19. |
| `m38_tabular_ops.md` | M38 | **`tabular` round-out — aggregations + group-by** (Phase 3 of the Pandas plan). Single big agent, 5 phase commits, ~2530 LOC, **zero STOP CRITERIA cuts**. Phase A: typed `df.get_column_i64/...` accessors + restored M37-cut comparison ops. Phase B: per-column aggregations (sum/mean/min/max/count/std/var/median); null-skipping; NaN propagates on f64. Phase C: `df.describe()`, `Column.fill_null`, `tabular.from_dict`. Phase D: new `GroupedDataFrame` class (M36 canonical path) + `df.group_by(cols)` + size/keys/sum/mean/min/max/count shortcuts + custom `agg(specs)`. Hash-based group-by with `\x01`-joined multi-column keys. Phase E: +25 tests + 1 demo (`examples/tabular_groupby_demo.spy`) + LANGUAGE_GUIDE §11.18+§11.19. NativeFn IDs 880-934. Four findings: Dict insertion-order (from_dict lex-sorts); NaN propagation vs nansum; null-keyed groups; Edit-tool worktree leak recurred from M37. Lesson 1 streak: 20. |
| `m39_tabular_reshape.md` | M39 | **`tabular` Phase 4 — reshape ops** (unique/value_counts/concat/merge/pivot/melt). Single agent, 4 phase commits, ~2430 LOC, **zero STOP CRITERIA cuts**. Phase A: 5 typed `unique_*` per dtype + `value_counts` + `concat_rows` + `concat_cols`. Phase B: `df.merge(other, on, how)` — hash-join inner/left/right/outer reusing M38's key encoding; null cells in `on` never match; merged `on` cols inherit rhs values on right-only outer rows. Phase C: `df.pivot(index, columns, values)` + `df.melt(id_vars, value_vars)`. Phase D: +25 tests + 1 demo + LANGUAGE_GUIDE §11.20+§11.21. NativeFn IDs 935-984 (11 used). Five findings: f64 unique uses `to_bits()` (HashSet<f64> doesn't compile); null-cell join keys short-circuit to None; merge `on` cols inherit rhs on outer; melt pre-reads value_vars to avoid virtual-call-per-cell; Edit-tool worktree leak confirmed-recurring across M37+M38+M39. Lesson 1 streak: 21. |
| `m40_tabular_timeseries.md` | M40 | **`tabular` Phase 5 — time series + cumulative + null + iloc**. Single agent (after a transient 529 killed the first launch with zero state), 4 phase commits, ~2175 LOC, **zero STOP CRITERIA cuts**. DatetimeIndex explicitly deferred to M41. Phase A: 8 cumulative numeric ops with "propagate from first null" rule; `dropna`/`dropna_subset`; 5 per-dtype `fillna_*`; `iloc` half-open. Phase B: 10 rolling-window ops (sum/mean/min/max/std × i64+f64); leading nulls; sample n-1 std. Phase C: `df.resample(time_col, rule, agg)` with `<i64><m|h|d>` rule parser; `df.asof_merge` via `Vec::partition_point` after stable-sort. Phase D: +28 tests + 1 demo + LANGUAGE_GUIDE §11.22-§11.25. NativeFn IDs 985-1012 (28 used). Six findings — most consequential: **Edit-tool worktree leak narrowed to Edit-on-existing-files; Write with absolute paths is unaffected** — first cause-narrowing across 4 milestones. Lesson 1 streak: 22 — four consecutive tabular-package single-agent milestones (M37/M38/M39/M40) shipped clean. |
| `m41_tabular_index.md` | M41 | **`tabular` Phase 5b — DatetimeIndex (minimum viable) + pivot_table**. Single agent, 2 commits (per-phase-cadence slip — see methodology nuance), ~2193 LOC, **zero STOP CRITERIA cuts**. Closes the M40 DatetimeIndex deferral with the minimum viable shape. Phase A: DataFrame payload 24 → 40 bytes (optional index + index_name; 3 constructors updated); 6 index methods. Phase B: index-aware `resample_index` / `asof_merge_index` / 3 per-dtype `select_by_label_*`. Phase C: `pivot_table(index_col, columns_col, values_col, aggfunc)` — pivot+group_by+agg in one. Phase D: +25 tests + 1 demo + LANGUAGE_GUIDE §11.26-§11.28. NativeFn IDs 1015-1026 (12 used). **EXPLICIT scope-down (M42 anchor)**: existing methods that return a fresh frame drop the index in v1; only sort_index/resample_index/asof_merge_index/select_by_label_* preserve it. Five findings, incl. **first per-phase-cadence nuance to Lesson 1 brief language since M28 escalation** (phases sharing cross-cutting infrastructure can land as one commit). Edit-tool leak recurred once; cp-recovered in ~30 seconds. Lesson 1 streak: 23. |
| `m42_tabular_index_propagation.md` | M42 | **`tabular` index propagation through existing methods** — closes the M41 v1 scope-down. Single agent, **5 separable per-phase commits + Phase E**, ~1693 LOC, **zero STOP CRITERIA cuts**. The 11 existing DataFrame methods that returned a fresh frame (filter / sort_by / head / tail / iloc / select / drop / rename / dropna / dropna_subset / fillna_* / merge) now PROPAGATE the index. **Pattern**: one helper `m42_permute_index_into_df` (+ sibling `m42_copy_index_into_df`) applied at the emit site of each handler — 280 LOC added to `builtins.rs` (4 helpers + 11 emit-call swaps). **No new NativeFn IDs**. Merge index policy per `how`: lhs wins inner/left/outer, rhs wins right, outer-with-dtype-mismatch → RangeIndex fallback. **First test-flip in project history**: filter_drops_index → filter_preserves_index_m42 (assertion flipped from `has=false` to `has=true`). **M41/M42 cadence contrast confirms the streak nuance**: shared-infra → combined commit OK; disjoint-handler → clean per-phase. Edit-tool leak hit 5× (back up from 1× in M41 because of many small per-phase Edits to `builtins.rs`); all `cp`-recovered in seconds. Lesson 1 streak: 24. |
| `m43_tabular_index_reshape.md` | M43 | **`tabular` reshape index propagation — closes v1 single-index story**. Single agent, 4 separable per-phase commits, ~1715 LOC, **zero STOP CRITERIA cuts**. pivot_table promotes `index_col`; single-column group_by promotes the key (multi-column unchanged, M44 with MultiIndex); pivot promotes index; concat_rows concatenates indexes when dtype+name match; concat_cols lhs wins; melt repeats input index per value_var. **Two methodology data points**: (1) test-flip cascade was 9 vs brief's 2-4 estimate — M38's 6 group_by tests cascaded because group_by was M38's headline feature; lesson: estimate flip count by grepping existing tests, not intuition. (2) Edit-tool worktree leak broader than M40 narrowing claimed — M43 saw Write also leak at first-edit-per-file boundaries (M40-M42 thought Edit-only); ~15 cp recoveries in ~90 seconds. Updated workaround: precautionary cp at session start. After M43 the `tabular` package is fully index-aware end-to-end for single-column indexes; MultiIndex (M44) remains the headline architectural lift. Lesson 1 streak: 25 — **seven consecutive `tabular` package agents** (M37-M43). |
| `m44_tabular_multiindex.md` | M44 | **`tabular` MultiIndex (M44a)** — the headline architectural lift after M43. Single agent, 4 separable per-phase commits, ~1500-2000 LOC, **zero STOP CRITERIA cuts**. DataFrame payload 40 → 56 bytes for optional `index_levels: List[Column]?` + `index_names: List[str]?` (mutually exclusive with M41 single-col). Phase A: 6 new methods (NativeFns 1027-1032): set_index_multi / reset_index_multi / index_nlevels / index_level / index_level_name / sort_index_multi. Phase B: all 8 group_by aggregations dispatch on key count — single-col → M41 path; multi-col (≥2 keys) → new MultiIndex path with all keys promoted to index levels. Phase C: minimal MultiIndex propagation through filter / head / tail / iloc via new auto-dispatching helper `m44_permute_multiindex_into_df`. **All other ops drop a MultiIndex back to RangeIndex (M44b anchor)**. Phase D: +27 net tests + 1 demo + LANGUAGE_GUIDE §11.32. **Two methodology wins**: (1) **precautionary `cp` workaround eliminated the Edit-tool leak entirely** — zero recoveries mid-session vs M43's ~15; orchestrator-side integration also clean. Cleanest tabular integration in the series. (2) **Shared-infra cadence exception worked cleanly when classified explicitly** — brief predicted 30-50% first-commit window; agent landed at ~35%. 1 M43 contract test flipped (multi_col_group_by_does_not_promote_to_index → multi_col_group_by_promotes_to_multiindex_m44). Lesson 1 streak: 26 — **eight consecutive `tabular` package agents** (M37-M44). |
| `m45_tabular_multiindex_propagation.md` | M45 | **`tabular` full MultiIndex propagation through M42 + M43 ops** — lifts the M44 v1 scope-down. Single agent, 3 separable per-phase commits (disjoint-handler — first commit at ~20%), ~1300 LOC, **zero STOP CRITERIA cuts**. **Pattern**: route handler emit calls through M44's auto-dispatching helper `m44_permute_multiindex_into_df` + new sibling `m45_copy_multiindex_into_df` for column-list ops. Phase A: 8 M42 ops (sort_by/dropna/dropna_subset/fillna_*/merge/select/drop/rename) propagate MultiIndex; merge per-`how` policy extends to MultiIndex via new `m45_merge_build_multiindex` (inner/left/right preserve; outer dtype-mismatch still RangeIndex — M46 anchor). Phase B: 5 M43 ops — melt repeats each MultiIndex level per value_var; concat_rows uses new `m45_concat_rows_multiindex` with 3-tier fallback (MultiIndex → single-col → RangeIndex); concat_cols lhs MultiIndex wins; pivot + pivot_table explicitly drop MultiIndex (reshape row dim — no clean target). Phase C: +19 net tests + 1 demo + LANGUAGE_GUIDE §11.26+§11.32 rewrites. **Tests flipped (2 — exactly as predicted)**: sort_by + select drops_multiindex_m44b_anchor → preserves_multiindex_m45. **Methodology data point**: agent couldn't run the precautionary cp block (Bash + PowerShell both denied at session start) yet **zero leak recurrences** — refined hypothesis: leak triggers on worktree divergence at Edit-session start, not on first-Edit-on-existing-file as M40 narrowed. After M45 tabular is **fully index-aware for both single-col AND MultiIndex end-to-end**. Lesson 1 streak: 27 — **nine consecutive `tabular` package agents** (M37-M45). |
| `m46_tabular_extensions.md` | M46 | **`tabular` stack/unstack + df.loc range + outer-merge MultiIndex + time-series MI + extensions** — cleanup-and-polish after M45. Single agent, 5 separable per-phase commits (disjoint-handler — first commit at ~20%), ~2358 LOC, **zero STOP CRITERIA cuts**. **After M46 the `tabular` v1 surface is functionally complete except for v0.4 polish.** Phase A: stack/unstack (with must-share-dtype + must-have-MultiIndex constraints). Phase B: 5 per-dtype `loc_range_*` (extends M41 select_by_label_* one-row to inclusive range). Phase C: outer-merge dtype-mismatch produces NaN-padded 2-level MultiIndex (replaces M42 RangeIndex fallback); set_index_list ergonomic unification; pivot_table_aggfunc_list + pivot_table_margins. Phase D: resample/resample_index drop MultiIndex; asof_merge/asof_merge_index preserve lhs MultiIndex. Phase E: +27 tests + 1 demo + LANGUAGE_GUIDE §11.32 rewrite + §11.33/§11.34 new. NativeFn IDs 1033-1042 (10 new). **Methodology data point — M45 hypothesis REFUTED**: agent saw the Edit-tool leak return under the same conditions as M45 (Bash denied for cp loop form, main was sync'd post-M45-push). M45 was the lucky outlier, not a stable improvement. Honest current state: cause is unknown; leak is intermittent. Workaround stays well-routinized. The M45/M46 cycle is itself a methodology data point — don't extrapolate from one milestone when the phenomenon is intermittent. **Tests flipped (0)**. Lesson 1 streak: 28 — **ten consecutive `tabular` package agents** (M37-M46). |
| `m47_tabular_polish.md` | M47 | **`tabular` v0.4 polish: iloc 2-D + negative iloc + rolling Welford/min_periods + ColumnCategorical**. Single agent, 2 commits (Phase A+B+C combined at ~70% of budget + Phase D), ~2309 LOC, **zero STOP CRITERIA cuts**. **First new Column subclass since M37** (`ColumnCategorical` — 32-byte payload codes + categories + nulls + length). Phase A: iloc_2d + negative iloc (lifts M40 v1 rejection). Phase B: 10 rolling_*_min_periods + Welford std internal upgrade. Phase C: ColumnCategorical via to_strings() coercion in v1 (optimized codes paths deferred to M48). Phase D: +32 tests + 1 demo + LANGUAGE_GUIDE §11.35/§11.36 new. NativeFn IDs 1043-1060 (18 new). **BIG methodology lesson — new cadence classification "cross-dispatch"**: brief miscategorized M47 as disjoint-handler (predicting 20%); agent's first commit landed at ~70% because adding a new sealed-class subclass forces every dispatch file (resolver/ir/native/builtins) to compile together. Three cadence classifications now established across M41/M42/M43/M44/M45/M46/M47 septet: disjoint-handler (~20%), shared-infra (~30-50%), **cross-dispatch (NEW: ~50-75% for new sealed-class subclass)**. Future briefs classify accordingly; M48's categorical-codes-optimization is disjoint-handler since the class already exists. Streak holds at 29 — agent committed cleanly without orchestrator intervention. Tests flipped (1): iloc_negative_start_raises → iloc_negative_start_works_m47. Edit-tool leak: did NOT recur (M45/M46/M47 alternation no/leak/no confirms intermittence). Stdlib classes 18 → 19. Lesson 1 streak: 29 — **eleven consecutive `tabular` package agents** (M37-M47). |
| `m48_tabular_bench.md` | M48 | **`tabular` vs pandas 3.0 comprehensive benchmark suite — third benchmark-only milestone (after M7-fair + M26-extended)**. Pure bench infrastructure (~2193 LOC of Python harness + CSV gens + .spy/.py pairs + JSON snapshot + Markdown report); NO Rust changes. Single agent, 4 separable per-phase commits (disjoint-handler — first commit at ~20%). 43 cells (37 timed + 6 documented skips): 8 core ops × 4 sizes (small/medium/large/xl-skipped) + 7 categorical-specific + memory peaks. **Headline findings**: geomean 0.30× (StrictPy / pandas); 28W/1T/8L. StrictPy wins on all small cells (pandas import ~1s dominates) + read/filter/sort/rolling/describe/unique at medium+. Pandas wins decisively on hash-bucketing at medium+: group_by_str 11.2×, group_by+sum 11.25×, pivot_table 22.2×. **M49 target now numeric**: drive group_by_cat_via_strings at medium from 12.8s → <1.5s (~10× speedup via direct codes-hash). **Surprise**: pandas Categorical at low cardinality (8 values) gave 0.98× speedup — codes-hash shines at high cardinality; M49 must add ~5000-value fixture. **Memory**: StrictPy peak RSS 4-5× pandas at large; queued as M48b memory deep-dive. **STOP CRITERIA cuts**: xl size + 8 large cells skipped (>30 min timeouts) — documented as `skip` rows. Tests 993 → 993 (no Rust changes). Three benchmark suites now: canonical 16-cell (M7-M11), extended 30-cell (M26), tabular 37-cell (M48). Lesson 1 streak: 30 — **twelve consecutive `tabular`-related agents** (M37-M48). |
| `m49_tabular_codes.md` | M49 | **`tabular` categorical codes optimization + polish — MASSIVE bench-validated win**. M48 predicted ~10× speedup; M49 actually delivered **~70-194×** depending on cardinality. M48 baseline group_by_cat_via_strings at medium = 12.8s; **M49 = 66ms (~194×)**. At medium_card_5000 (high cardinality): 5.4s → **77ms (~70×)**. StrictPy now beats pandas's own Categorical fastpath by ~14× at high cardinality — **most dramatic single-milestone perf win since the M8 JIT cliff** (931ms → 14.6ms on fib(30)). Single agent, 5 separable per-phase commits (disjoint-handler — first commit at ~20%), ~2700 LOC, **zero STOP CRITERIA cuts**. Phase A: high-cardinality bench fixture (medium_card_5000) + baseline (verification gate). Phase B: codes-hash for group_by — m38_groupby_* detects ColumnCategorical key columns and hashes on codes[i] (i64) directly instead of to_strings() materialization. Single-col + multi-col + mixed-dtype-fallback all handled — **transparent, no API change**. Phase C: codes-hash for merge (requires bit-identical categories[] arrays); new constructors `col_categorical_ordered(values, categories)` + `col_categorical_from_codes(codes, categories)`; new predicate `cc.is_ordered()`. Phase D: 1w/1M/1Y resample rules (calendar arithmetic + end-of-month clamping); outer-merge MultiIndex on either side (extends M46 dtype-mismatch fallback to all 3 cases); unstack distributes EVERY regular column (M46 only first); per-dtype `loc_range_multi_{i64,str,datetime}` for innermost MultiIndex level. Phase E: +23 tests + 1 demo (~230 LOC) + bench rerun + LANGUAGE_GUIDE §5 + §11.36/§11.37/§11.38 new. NativeFn IDs 1061-1066 (6 new). **Bench-first methodology validated**: M48 + M49 establishes the template for fixing known perf gaps (second bench-first cycle in the project after M7-unfair → M7-fair → M8). Edit-tool leak recurred ~10× but defensive cp protocol worked. Lesson 1 streak: 31 — **thirteen consecutive `tabular`-related agents** (M37-M49). Largest disjoint-handler milestone to date. |
